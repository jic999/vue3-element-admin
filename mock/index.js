// import Mock from 'mockjs'
// const modules = import.meta.glob('./api/*.js', { eager: true })
//
// Mock.setup({
//   timeout: '150-350',
// })
// Object.keys(modules).forEach((key) => {
//   const apiList = modules[key].default
//   apiList.forEach((api) => Mock.mock(api.url, api.method, api.response))
// })
